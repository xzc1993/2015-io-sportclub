<%= form_for ([ @course, @course_event ]) do |f| %>
  <% if @course_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_event.errors.count, "error") %> prohibited this course_event from being saved:</h2>

      <ul>
      <% @course_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
